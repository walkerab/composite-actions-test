name: Plan / Test On PR

on:
  pull_request:
    types: [opened, synchronize, edited, reopened]
    branches:
      - main

jobs:
  do_something:
    name: Do Stuff
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v2
      - id: foo
        uses: ./.github/comment-on-pr
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          folder-to-check: stage